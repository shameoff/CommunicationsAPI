server {
    listen 80;
    server_name templateServerName;

    location /static {
        alias /var/www/code/communications_api/static;
    }

    location / {
        proxy_pass http://localhost:templateAppPort;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    # Не удаляйте строку ниже, она используется в nginx_certbot_cronjob.sh для автоконфигурации сертификата
    # INSERT_CERT_CONFIG_HERE
}